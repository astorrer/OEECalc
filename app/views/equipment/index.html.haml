.jumbotron
  %h1.display-4 Equipment
  %p Remember, a facility must exist before you can add equipment to it. Create a new facility using the facilities manager if neccessary. 

.card
  .card-header
    = link_to new_equipment_path, class: 'btn btn-success' do
      = fa_icon 'plus'
      Add Equipment
  .card-body
    %table.table
      %thead
        %tr
          %th Name
          %th Number
          %th Facility
          %th{:colspan => "3"}
      %tbody
        - @equipment.each do |equipment|
          %tr
            %td= equipment.name
            %td= equipment.number
            %td= equipment.facility.name
            %td
              .btn-group
                = link_to equipment, class: 'btn btn-primary' do
                  = fa_icon 'eye'
                  View
                = link_to edit_equipment_path(equipment), class: 'btn btn-primary' do
                  = fa_icon 'pencil'
                  Edit
                = link_to equipment, method: :delete, data: { confirm: 'Aare you sure?' }, class: 'btn btn-danger' do
                  = fa_icon 'trash'
                  Delete

  .card-footer
    Equipment Manager


